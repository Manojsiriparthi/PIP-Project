# Reusable VPC Module

The module creates the complete networking/security layer: VPC, public/app/data subnets, IGW, NAT Gateway(s), route tables, SGs, NACLs, S3/SQS/SNS endpoints, S3 flow-log bucket, and VPC Flow Logs.

Environment-specific configuration stays outside the module.
